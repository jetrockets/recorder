#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'

bundle install

# spec/rails_helper.rb runs the migrations, so the database only has to exist.
# test is the only environment spec/dummy/config/database.yml defines.
RAILS_ENV=test bundle exec rake -f spec/dummy/Rakefile db:create
